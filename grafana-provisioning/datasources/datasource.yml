apiVersion: 1
datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb-server:8086
    jsonData:
      version: Flux
      organization: my-org
      defaultBucket: github-metrics
    secureJsonData:
      token: my-super-secret-token
    isDefault: true